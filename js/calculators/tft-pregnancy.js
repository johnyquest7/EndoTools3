export default class TftPregnancy {
  constructor(){ this.name='TFTs in pregnancy'; }
  render(container){
    container.innerHTML = `<iframe class="reference" src="pregnancy_tsh.html" title="TFT in pregnancy"></iframe>`;
  }
}
